services:
  shepherd-rm:
    build:
      context: .
    environment:
      SHEPHERD_DATABASE_URL: postgresql://${POSTGRES_USER:-shepherd_rm}:${POSTGRES_PASSWORD:-shepherd_rm_local}@postgres:5432/${POSTGRES_DB:-shepherd_rm}
      SHEPHERD_HOST: 0.0.0.0
      SHEPHERD_PORT: 8000
      SHEPHERD_LOG_LEVEL: ${SHEPHERD_LOG_LEVEL:-INFO}
      SHEPHERD_OPENAPI_PATH: /app/openapi/openapi.yaml
    ports:
      - "${SHEPHERD_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://localhost:8000/health')
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-shepherd_rm}
      POSTGRES_USER: ${POSTGRES_USER:-shepherd_rm}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-shepherd_rm_local}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - shepherd_postgres_data:/var/lib/postgresql/data

volumes:
  shepherd_postgres_data:

# Python SDK

The supported Python client package will live here. It will consume the public OpenAPI contract and provide a small ergonomic layer for resource, lease, secret, and identity operations.
